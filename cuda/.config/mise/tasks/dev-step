#!/usr/bin/env bash
#MISE description="Internal: build, then deploy"
#MISE hide=true
set -euo pipefail

if [ "$#" -lt 1 ]; then
  echo "usage: mise dev -- <app> [args]" >&2
  exit 2
fi

app=$1
shift
mise run build -- "$app"
mise run deploy -- "$app" "$@"
